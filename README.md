# serverless-user-service
Serverless User Service is a lightweight, event-driven API built on AWS using NestJS and Lambda. Clean serverless architecture, cost-efficient design, and infrastructure as code using Terraform.  Authentication is handled at the API Gateway level via JWT authorizers, while the service is responsible for authorization and business logic.
